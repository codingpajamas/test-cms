extends ../../layout-admin

block content
	h4
		| Delete User

	p
		| Are you sure you want to delete this user?
	form.text-center(method="POST", action="/admin/users/delete")
		input(type="hidden", name="id", value="#{user.id}")
		button.btn.btn-danger
			| Delete